74: <button type="button" onClick={() => updateField(field.id, { options: field.options.filter((_, fi) => fi !== i) })} className="p-1.5 text-slate-300 hover:text-red-500 rounded-lg">Ã—</button>
833: <option value="INR">₹ INR (Indian Rupee)</option>
835: <option value="EUR">€ EUR (Euro)</option>
836: <option value="GBP">£ GBP (British Pound)</option>
864: {activeForm.paymentConfig?.currency === 'INR' ? '₹' : activeForm.paymentConfig?.currency === 'EUR' ? '€' : activeForm.paymentConfig?.currency === 'GBP' ? '£' : '$'}
992: <div className="w-full px-4 py-3 rounded-xl bg-white border border-transparent shadow-[0_2px_10px_rgba(0,0,0,0.03)] text-sm text-slate-400 font-medium font-mono">0 {field.minValue !== undefined && field.maxValue !== undefined ? `(${field.minValue}â€“${field.maxValue})` : ''}</div>
996: <div className="flex-1 py-3 rounded-xl bg-white border-2 border-transparent shadow-sm text-sm font-bold text-slate-400 text-center">✅ Yes</div>
997: <div className="flex-1 py-3 rounded-xl bg-white border-2 border-transparent shadow-sm text-sm font-bold text-slate-400 text-center">âŒ No</div>
1068: â† Back
1072: {isLast ? <><Send width={14} /> Submit</> : <>Next →</>}
1235: <p className="font-bold text-sm text-sky-600 dark:text-sky-400">âœ“ {activeForm.automation.whatsappTemplate.headerFileName}</p>
1256: <div className="hidden sm:block font-bold text-slate-400 dark:text-slate-500">→</div>
1322: <span>{btn.type === 'URL' ? '🔗' : '⚡'}</span>
1325: <div className="hidden sm:block font-bold text-slate-400 dark:text-slate-500">→</div>
1405: <label className="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-3 block">{card.headerType === 'VIDEO' ? '🎬 Card Video' : 'ðŸ–¼ï¸ Card Image'}<span className="text-red-400 ml-1">*</span></label>
1482: <span className="text-[10px] text-violet-500 font-bold truncate block">⚡ {btn.text} <span className="text-slate-400 font-normal">({btn.type})</span></span>
1546: {/* Phone frame â€” identical to form design tab */}
1621: {card.buttons?.length > 0 && <div className="mt-auto border-t border-slate-200 dark:border-white/10">{card.buttons.map((btn, bi) => <div key={bi} className="px-1 py-1 text-[9px] text-[#00a884] font-bold text-center border-b border-slate-100 dark:border-white/5 last:border-0 truncate flex items-center justify-center gap-0.5">{btn.type === 'URL' && '↗'}{btn.text}</div>)}</div>}
1634: {btn.type === 'URL' && '↗'}{btn.text}
1643: <span className="text-sm opacity-60">😊</span>
1732: r.contactNumber || 'â€”',
1735: return Array.isArray(v) ? v.join(', ') : (typeof v === 'object' && v !== null ? Object.values(v).join(', ') : (v || 'â€”'));
1778: <td className="px-4 py-3 font-medium text-slate-700 dark:text-slate-300 whitespace-nowrap">{r.contactNumber || <span className="text-slate-300 dark:text-slate-600">â€”</span>}</td>
1781: let display = 'â€”';
1783: else if (typeof v === 'object' && v !== null) display = Object.entries(v).map(([k, val]) => `${k}: ${val}`).join(' Â· ');